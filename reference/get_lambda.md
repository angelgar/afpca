# Estimate the Adaptive Penalty Matrix (Lambda)

Internal function to estimate the Adaptive penalty matrix

## Usage

``` r
get_lambda(coef, n.comp, N.Unp.Basis)
```

## Arguments

- coef:

  Coefficients

- n.comp:

  Number of FPCs to estimate

- N.Unp.Basis:

  Number of Unpenalized Basis
